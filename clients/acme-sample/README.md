# Client brief

Client name: ACME Sample

Use this folder as a reference only.
Replace it with a real client folder when starting a new project.

Checklist:
- place original files in `raw/`
- store cleaned data in `derived/`
- keep notebooks in `notebooks/`
- publish final HTML in `dashboard/`
- store delivery files in `exports/`
