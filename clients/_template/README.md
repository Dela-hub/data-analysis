# Client template

Copy this folder to start a new client workspace.

Suggested command:
- `cp -r clients/_template clients/<client-name>`

Then add:
- client brief
- source data
- notebooks
- dashboard outputs
